import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.roomrescue.game',
  appName: 'Room Rescue',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;